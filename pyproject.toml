[project]
name = "express"
version = "0.1.3"
description = "看README"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "boto3==1.42.9",
    "dill>=0.4.0",
    "pydantic>=2.12.5",
    "pytest>=9.0.2",
    "rich>=14.2.0",
    "simple-file-checksum>=1.3.1",
    "tqdm>=4.67.1",
    "typer>=0.20.0",
]
[tool.setuptools.packages.find]
where = ["."]
include = ["express"]


[project.scripts]
express="express.cli:app"
